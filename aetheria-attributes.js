(()=>{'use strict';const A=['gorgeous','simple','elegant','lively','mature','cute','sexy','pure','warm','cool'],L={gorgeous:'Quý phái',simple:'Đơn giản',elegant:'Thanh lịch',lively:'Năng động',mature:'Trưởng thành',cute:'Dễ thương',sexy:'Gợi cảm',pure:'Trong sáng',warm:'Giữ ấm',cool:'Mát mẻ'},E=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),grades=()=>window.__aetheriaAttributeGrades||{},rows=id=>A.map(k=>[k,grades()[String(id)]?.grades?.[k]||'—']);function card(c){if(c.querySelector('.real-attr-row'))return;const g=rows(c.dataset.itemId).filter(v=>v[1]!=='—');if(!g.length)return;const b=c.querySelector('.item-body');if(!b)return;const e=document.createElement('div');e.className='real-attr-row';e.innerHTML=g.slice(0,3).map(v=>`<span><b>${E(v[1])}</b>${E(L[v[0]])}</span>`).join('');b.appendChild(e)}function detail(){const d=document.querySelector('#itemDetailContent');if(!d||d.querySelector('.real-attr-panel'))return;const id=(d.querySelector('.detail-badge:nth-child(2)')?.textContent||'').replace(/\D/g,''),g=rows(id);if(!id||!g.some(v=>v[1]!=='—'))return;const e=document.createElement('section');e.className='real-attr-panel';e.innerHTML=`<div class="real-attr-head"><div><small>ĐỐI CHIẾU THUỘC TÍNH</small><h4>Hạng thuộc tính từ nguồn item Việt</h4></div><span>✓</span></div><div class="real-attr-grid">${g.map(v=>`<div><span>${E(L[v[0]])}</span><b class="g-${E(v[1])}">${E(v[1])}</b></div>`).join('')}</div><p>SS–E là hạng hiển thị từ nguồn công khai. Không quy đổi thành điểm gốc của VNG.</p>`;d.querySelector('.detail-main')?.appendChild(e)}function enhance(){document.querySelectorAll('[data-item-id]').forEach(card);detail()}let raf=0;function schedule(){cancelAnimationFrame(raf);raf=requestAnimationFrame(enhance)}function boot(){if(!window.__aetheriaAttributeGrades){window.addEventListener('aetheria:data-ready',boot,{once:true});return}const h=document.querySelector('#heroStatus'),cov=window.__aetheriaAttributeCoverage;if(h&&cov&&!h.querySelector('.attribute-coverage')){const n=document.createElement('div');n.className='attribute-coverage';n.textContent=`✦ Đối chiếu thuộc tính: ${(cov.withAnyAttributes||0).toLocaleString('vi-VN')}/${(cov.itemCount||0).toLocaleString('vi-VN')}`;h.appendChild(n)}schedule()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();const o=[];['#rankingGrid','#wardrobeGrid','#itemDetailContent'].forEach(s=>{const el=document.querySelector(s);if(!el)return;const m=new MutationObserver(schedule);m.observe(el,{subtree:true,childList:true});o.push(m)});window.__aetheriaAttributesObservers=o})()