(()=>{
'use strict';
const A=['gorgeous','simple','elegant','lively','mature','cute','sexy','pure','warm','cool'];
const L={gorgeous:'Quý phái',simple:'Đơn giản',elegant:'Thanh lịch',lively:'Năng động',mature:'Trưởng thành',cute:'Dễ thương',sexy:'Gợi cảm',pure:'Trong sáng',warm:'Giữ ấm',cool:'Mát mẻ'};
const CATS={hair:'Tóc',dress:'Váy liền',top:'Áo',bottom:'Quần',coat:'Áo khoác',socks:'Tất',shoes:'Giày',makeup:'Trang điểm',accessory:'Phụ kiện'};
let M=new Map();
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function fields(x){const attrs=A.map(k=>[L[k],+x[k]||0]).filter(a=>a[1]);const tags=Array.isArray(x.tags)?x.tags:(x.tags?[String(x.tags)]:[]);return `<div class="game-fields"><div class="game-field-title">Dữ liệu trong game</div><div class="game-field-grid"><div><small>Loại đồ</small><b>${esc(CATS[x.category]||x.category||x.typeName||x.type||'Chưa xác minh')}</b></div><div><small>Độ hiếm</small><b>${x.rarity?('★'.repeat(+x.rarity)):'—'}</b></div>${x.suit?`<div><small>Bộ / Trang phục</small><b>${esc(x.suit)}</b></div>`:''}<div><small>Mã item</small><b>${esc(x.id)}</b></div></div>${tags.length?`<div class="game-tags">${tags.map(t=>`<span>${esc(t)}</span>`).join('')}</div>`:''}<div class="game-attrs">${attrs.map(a=>`<span><b>${esc(a[0])}</b> ${a[1]}</span>`).join('')}</div>${x.source||x.sourceUrl?`<div class="game-source">Nguồn: ${esc(x.source||'Dữ liệu công khai')}${x.sourceUrl?` · <a href="${esc(x.sourceUrl)}" target="_blank" rel="noopener">mở nguồn</a>`:''}</div>`:''}</div>`}
function addToDetail(){const d=document.querySelector('#itemDetailContent');if(!d)return;const badge=[...d.querySelectorAll('.detail-badge')].find(e=>/ID\s*/i.test(e.textContent||''));const id=(badge?.textContent||'').replace(/\D/g,'');const x=M.get(id);if(!x||d.querySelector('.game-fields'))return;const wrap=d.querySelector('.detail-main');if(wrap)wrap.insertAdjacentHTML('beforeend',fields(x));}
function enhanceCards(){document.querySelectorAll('[data-item-id]').forEach(c=>{const x=M.get(String(c.dataset.itemId));if(!x||c.querySelector('.catalog-mini'))return;const body=c.querySelector('.item-body');if(!body)return;const tags=Array.isArray(x.tags)?x.tags:[];const text=(x.suit?`Bộ: ${x.suit}`:'')+(tags.length?` · ${tags.slice(0,2).join(' · ')}`:'');if(text)body.insertAdjacentHTML('beforeend',`<div class="catalog-mini">${esc(text)}</div>`);});}
async function boot(){try{const r=await fetch('./data/items.json?b=20260917r94',{cache:'no-store'});if(!r.ok)return;const d=await r.json();const arr=Array.isArray(d.items)?d.items:Array.isArray(d)?d:[];arr.forEach(x=>M.set(String(x.id),x));window.__aetheriaCatalog=M;enhanceCards();addToDetail();new MutationObserver(()=>{enhanceCards();addToDetail()}).observe(document.body,{subtree:true,childList:true});}catch(e){console.warn('[Aetheria game fields]',e)}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
